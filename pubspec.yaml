name: money_management
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # 他の依存関係をここに追加します

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # 使用していない場合はコメントアウトします
  # assets:
  #   - assets/images/
  #   - assets/icons/
